# Transfer Solution

Structure with backend (.NET 9 Web API + SignalR + EF InMemory) and frontend (Blazor WebAssembly).

- Frontend: http://localhost:3000
- Backend: http://localhost:5000 

To run with Docker:

```bash
docker-compose up --build
```
